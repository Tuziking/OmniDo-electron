# v1.0.4 Release Notes

## 🇨🇳 中文

**主要更新：时间轴动态范围、截止时间倒计时与优先级修复**

### ✨ 新特性与优化
1.  **截止时间实时倒计时**：
    *   **动态倒计时**：对于 24 小时内截止的任务，现在会显示秒级实时倒计时（如 `1h 30m 15s`），增强紧迫感。
    *   **混合显示逻辑**：3 天内的近期任务显示详细时间（如 `1d 5h`），远期任务显示简化天数（如 `4d`）。
2.  **时间轴动态范围**：
    *   时间轴视图现在会根据任务安排自动扩展可以查看的日期范围（默认 30 天，或自动包含最远期的任务），确保所有未来规划一览无余。

### 🐛 修复
1.  **Low 优先级显示修复**：
    *   修复了“低”优先级（Low）标签在任务卡片上不显示的问题，并添加了全新的蓝色系视觉标识。
2.  **远期任务显示修复**：
    *   修复了时间轴视图仅显示未来 14 天无法查看更远任务的问题。

---

## 🇺🇸 English

**Main Updates: Dynamic Timeline, Real-time Countdown & Priority Fixes**

### ✨ New Features & Improvements
1.  **Real-time Deadline Countdown**:
    *   **Live Ticker**: Tasks due within 24 hours now feature a live ticking countdown (e.g., `1h 30m 15s`) to highlight urgency.
    *   **Hybrid Display**: Deadlines within 3 days show detailed hours (e.g., `1d 5h`), while distant deadlines show a simplified day count (e.g., `4d`).
2.  **Dynamic Timeline Range**:
    *   The Timeline view now automatically extends its date range to include your furthest scheduled task (defaults to 30 days), ensuring all future plans are visible.

### 🐛 Fixes
1.  **Low Priority Visibility**:
    *   Fixed an issue where "Low" priority tags were hidden. Added a distinct blue visual style for Low priority tasks.
2.  **Distant Task Visibility**:
    *   Resolved a bug where the Timeline view was limited to 14 days, hiding tasks scheduled further in the future.
